# In MareNostrum, install these requirements in a separate directory 
# "packages/" in the root of the repository, using:
# >> module load hdf5
# >> module load python/3.12.1
# >> pip install -r requirements.txt --target=packages/
# After this, install the rest separately into $HOME/.local:
# >> pip install -e ../GridCal/src/GridCalEngine
# >> pip install -e ../stability_analysis
control==0.10.0
matplotlib==3.8.2
numpy==1.26.2
pandas==2.1.4
scikit-learn==1.3.2
scipy==1.11.4
ortools==9.8.3296
xlsxwriter
PyYAML
SciencePlots
# Older pandas version for issues detected in Windows users
# pandas==1.5.3
